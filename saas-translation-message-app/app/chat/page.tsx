import React from "react";

type ChatsProps = {
  params: {};
  searchParams: {
    error: string;
  };
};

const ChatsPage = ({ searchParams: { error } }: ChatsProps) => {
  return (
    <div>
      {/* Chat Permisson cat */}

      {/* ChatList */}
    </div>
  );
};

export default ChatsPage;
